// Room definitions are no longer hardcoded.
// Rooms are created dynamically by users at runtime.
// Any string entered by the user becomes a valid room name.
// The websocket backend (websocket_server.py) handles room creation automatically.
export {};
